apiVersion: v1
kind: Pod
metadata:
  name: pod3
  labels:
    app: helloworld
spec:
  containers:
  - name: k8s-demo
    image: kundan-prasad/k8s-demo
    ports:
    - name: nodejs-port
      containerPort: 3000
